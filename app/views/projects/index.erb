<h2>Patterns</h2>
<p>Welcome, <%= current_user.username %></p>
<% if logged_in? %>
  <ul>
    <% @projects.all.each_with_index do |project, i| %>
      <li><%= i + 1 %>. <a href="/projects/<%= project.id %>"><%= project.name %></a></li>
    <% end %>
  </ul>
    <% end %>
