<h2>Patterns shared by registered users</h2>
<p>Welcome, <%= current_user.username %></p>
<% if logged_in? %>
  <ul>
    <% @projects.each_with_index do |project, i| %>
      <li><%= i + 1 %>. <a href="/projects/<%= project.name %>"><%= project.name %></a></li>
    <% end %>
  </ul>
    <% end %>
