<h2>Projects</h2>
<p>Welcome, <%= current_user.username %></p>
<% if logged_in? %>
  <ul>
    <% @project.each_with_index do |project, i| %>
      <li><%= i + 1 %>. <a href="/projects/projects/<%= project.name %>"><%= project.name %></a></li>
    <% end %>
  </ul>
    <% end %>
