<h2>Patterns</h2>
<p>Welcome, <%= current_user.username %></p>
<% if logged_in? && current_user %>
  <ul>
    <% current_user.projects.all.each_with_index do |project, i| %>
      <li><%= i + 1 %>. <a href="/projects/<%= @user.projects %>"><%= project.name %></a></li>
    <% end %>
  </ul>
    <% end %>
